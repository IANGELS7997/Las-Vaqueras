---
description: Pending n8n post-payment workflow — do not forget to finish later
alwaysApply: true
---

# n8n: pago exitoso (pendiente)

Dejar el workflow **completo e inactivo**. No publicar ni ejecutar en producción hasta que el usuario lo pida.

- Workflow: [Las Vaqueras: pago exitoso](https://n8n.pureiangel.com/workflow/SKVvYOhKSnPj1ggY) (`SKVvYOhKSnPj1ggY`)
- Inactivo a propósito: Stripe no registra webhook de producción. Publicar = el trigger corre solo en cada `payment_intent.succeeded`.
- Credenciales Stripe/Supabase/Uber/Resend enganchadas. Nodo **Create Uber Direct Delivery desactivado** (no pedir courier en pruebas). Pickup: Rio de Janeiro 903, Panamericana, 31210 Chihuahua; tel `+52 614 413 6539`.
- Remitente transaccional (n8n/Resend): `Las Vaqueras <noreply@lasvaqueras.com.mx>`. Contacto del negocio en la web: `servicio@lasvaqueras.com.mx`.
- Dominio Resend `lasvaqueras.com.mx` verificado (`c6d3c473-7f71-42af-80ec-82bbe7d63d17`). El panel n8n sigue en n8n.pureiangel.com (infra de la plataforma, no el sitio del restaurante).
- Pedido se inserta al crear el PaymentIntent (`awaiting_payment`); tras el pago pasa a `pending`. Facturapi/CFDI no reintroducir salvo que lo pidan.
- **Promoción / cupón 2x1 PAUSADA** (`is_tenth = false`). El checkout pide correo para ticket + promo futura. **Recordar actualizar copy y lógica cuando definan la promoción.**

Cuando el usuario vuelva a pagos, Stripe Connect, Uber Direct, Resend o promociones, recuérdale este pendiente y la promo por definir **antes** de otra tarea.
